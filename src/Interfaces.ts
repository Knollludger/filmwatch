export class UltimatePoint {
  Ds: Array<number> = [];
  Score = [];
}

export class Point {
  x: number = -1;
  y: number = -1;

  constructor(x: number, y: number) {
    this.x = x;
    this.y = y;
  }
}

export class Goal {}
